//by using const we cannot change the value of a variable
//but in case of arrays or objects we can push new values into the constant

const nums = [];
nums.push(5);
console.log(nums);

const person = {};
person.name = 'John';
console.log(person);